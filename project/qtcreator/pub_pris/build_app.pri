#-------------------------------------------------
#
# QtCreator's nornam app project's private attributions
#
# date:   2013-11-28
# version:v0.1(project template version)
#-------------------------------------------------

# build as an app
TEMPLATE = app
CONFIG -= app_bundle
CONFIG -= qt
CONFIG += thread

# project path definition
include(path.pri)

# dest dir for apps
DESTDIR = $$PTVAR_BIN
